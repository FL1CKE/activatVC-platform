{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "activatVC-platform/activatVC-agents-platform/Dockerfile"
  },
  "deploy": {
    "startCommand": "alembic upgrade head && python -m app.scripts.seed_agents && uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
